{% extends 'layout.html.twig' %}
{% do add_active_path(path('app_category_index')) %}

{% block body %}

    <h1 class="mb-4">{{ 'category.create.title'|trans }}</h1>

    {{ include('category/form.html.twig') }}

{% endblock body %}
